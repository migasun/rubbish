# URL of the Cloudflare Worker proxy
VITE_API_BASE_URL=https://steep-smoke-0e4c.vega-0b1.workers.dev
# When `quasar dev` is run the app connects to http://localhost:8787 by default.
# Set this value only if you deploy your own worker.
