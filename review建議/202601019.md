1. 代碼組織
src/pages/IndexPage.vue (861行) → 拆分為：
├── components/ServiceStatus.vue (頂部狀態卡片)
├── components/RouteTabs.vue (路線選項卡)
├── components/RefreshProgress.vue (自動重新載入進度條)
└── composables/useRouteData.js (數據加載邏輯)
2. 地圖組件重構
目前有兩個地圖組件但功能重疊：
- RouteMap.vue (Google Maps - 需 API Key)
- OpenStreetMapView.vue (OpenStreetMap - 免費)
建議: 統一使用 OpenStreetMap，刪除 Google Maps 組件
      一定要用leaflet 2.0.0-alpha以上 , 不要用1.9

3. 生產環境優化
// 移除所有 console.log
if (import.meta.env.PROD) {
  console.log = () => {}
}

4. 錯誤處理增強
// 添加全局錯誤處理
app.config.errorHandler = (err) => {
  console.error('Global error:', err)
  // 可發送到錯誤追蹤服務
}
 
5. 測試覆蓋率
目前測試較少，建議添加：
- 組件單元測試
- Store 狀態測試
- API mock 測試

7. TypeScript 遷移
typescript: ^5.0.0,
@vue/compiler-sfc: ^3.3.0

8. 性能優化
// 添加防抖優化
import { debounce } from 'lodash-es'
const debouncedLoad = debounce(loadData, 300)

9. 環境變量管理

# .env.local (開發)
VITE_API_BASE_URL=http://localhost:8787
VITE_AUTO_RELOAD_INTERVAL=30

# .env.production (生產)
VITE_API_BASE_URL=https://steep-smoke-0e4c.vega-0b1.workers.dev
VITE_AUTO_RELOAD_INTERVAL=60

10. 可訪問性
<!-- 添加 ARIA 標籤 -->
<button aria-label="重新載入垃圾車位置" @click="refresh">
  <q-icon name="refresh" />
</button>

11. 代碼風格統一
// 添加 ESLint + Prettier
eslint: ^8.50.0,
prettier: ^3.0.0

12. 數據緩存策略
// 添加 SWR 策略
const useSWR = () => {
  const data = ref(null)
  const isLoading = ref(false)
  
  async function fetchWithCache() {
    const cached = localStorage.getItem('routeData')
    if (cached) data.value = JSON.parse(cached)
    
    const fresh = await loadData()
    localStorage.setItem('routeData', JSON.stringify(fresh))
    return fresh
  }
}